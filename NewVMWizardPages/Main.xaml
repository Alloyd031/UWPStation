<Page
    x:Class="UWPStation.NewVMWizardPages.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPStation.NewVMWizardPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    Width="480"
	Height="500">

    <Grid>
        <Grid VerticalAlignment="Top">
            <TextBlock Text="Welcome to the New Virtual Machine Wizard" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,8,0,0"/>
            <TextBlock Text="What type of configuration you want?" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,32,0,0"/>
        </Grid>
        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,56,0,0">
            <RadioButton x:Name="TypicalRadioButton" Content="Typical (Recommended)" VerticalAlignment="Top" IsChecked="True"/>
            <TextBlock Text="Create a UWPStation 0.1.x virtual machine in a few easy steps." HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,32,0,0"/>
        </Grid>
        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,112,0,0">
            <RadioButton x:Name="CustomRadioButton" Content="Custom (Advanced)" VerticalAlignment="Top"/>
            <TextBlock Text="Create a virtual machine with advanced options." HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,32,0,0"/>
        </Grid>
        <Button Width="44" Height="44" Margin="10,446,0,0" VerticalAlignment="Top"
                ToolTipService.ToolTip="Help">
            <FontIcon Glyph="&#xE11B;"/>
        </Button>
        <Button x:Name="NextButton" Width="44" Height="44" Style="{StaticResource AccentButtonStyle}" Margin="426,446,0,0" VerticalAlignment="Top" Click="NextButton_Click"
                ToolTipService.ToolTip="Next">
            <FontIcon Glyph="&#xE111;"/>
        </Button>
    </Grid>
</Page>
