<ContentDialog
    x:Class="UWPStation.Dialogs.TakeSnapshotDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPStation.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <ResourceDictionary>
            <x:Double x:Key="ContentDialogMaxWidth">1100</x:Double>
            <Thickness x:Key="ContentDialogPadding">0</Thickness>
        </ResourceDictionary>
    </ContentDialog.Resources>

    <Grid
			x:Name="ContainerGrid"
		    Width="440"
			Height="500"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch">
        <Grid VerticalAlignment="Top" Margin="0,10,0,0">
            <FontIcon Glyph="&#xE121;" FontSize="60" HorizontalAlignment="Center" VerticalAlignment="Top"/>
            <TextBlock Text="Taking a snapshot lets you preserve the state of the virtual machine so that you can return to the same state later." Style="{StaticResource CaptionTextBlockStyle}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0"/>
        </Grid>
        <TextBlock Text="Name:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,113,0,0"/>
        <TextBox Text="Snapshot 1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="55,107,0,0" Width="375"/>
        <TextBlock Text="Description:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,143,0,0"/>
        <RichEditBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="88,144,0,0" Width="342" Height="297"/>
        <Button x:Name="TakeSnapshotButton" Width="44" Height="44" Margin="337,446,0,0" VerticalAlignment="Top"
                ToolTipService.ToolTip="Take Snapshot">
            <FontIcon Glyph="&#xE109;"/>
        </Button>
        <Button x:Name="CancelButton" Width="44" Height="44" Click="CancelButton_Click" VerticalAlignment="Top"
                ToolTipService.ToolTip="Cancel" Margin="386,446,0,0">
            <FontIcon Glyph="&#xE10A;"/>
        </Button>
        <TextBlock Text="You can't take snapshots at this time." Style="{StaticResource CaptionTextBlockStyle}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30,457,0,0"/>
    </Grid>
</ContentDialog>
