<Page
    x:Class="UWPStation.VMPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPStation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    Loaded="Page_Loaded">

    <Grid>
        <Grid Grid.Row="2" Grid.RowSpan="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="0,0,0,40">
            <Button x:Name="WelcomeButton" Background="Transparent" BorderBrush="Transparent" Height="100" Width="100" Margin="10,10,0,0" VerticalAlignment="Top" Click="WelcomeButton_Click">
                <Grid Margin="-50,-50,-50,-50">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE946;" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,33,0,0"/>
                    <TextBlock Text="Welcome" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,62,0,0"/>
                </Grid>
            </Button>
        </Grid>
        <Grid x:Name="Taskbar" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Grid.Row="3" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Button Height="40" Width="40" Background="Transparent" BorderBrush="Transparent" x:Name="StartButton" ToolTipService.ToolTip="Start">
                <FontIcon FontFamily="HoloLens MDL2 Assets" Glyph="&#xE782;" FontSize="16"/>
            </Button>
        </Grid>
    </Grid>
</Page>
