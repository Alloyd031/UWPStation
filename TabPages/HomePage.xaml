<Page
    x:Class="UWPStation.TabPages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPStation.TabPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True">

    <Grid>
        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="UWPStation" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,19,0,0"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,94,0,0">
            <Button Width="150" Click="CreateVM_Click" Height="150" VerticalAlignment="Top" HorizontalAlignment="Center">
                <Grid>
                    <FontIcon Glyph="&#xECC8;" Margin="0,-28,0,28" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="Create a New Virtual Machine" TextWrapping="Wrap" Margin="0,26,0,0" Height="38" VerticalAlignment="Top"/>
                </Grid>
            </Button>
            <AppBarSeparator/>
            <Button Width="150" Click="OpenVM_Click" Height="150" HorizontalAlignment="Center" VerticalAlignment="Top" >
                <Grid>
                    <FontIcon Glyph="&#xE8A7;" Margin="0,-28,0,28" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="Open a Virtual Machine" TextWrapping="Wrap" Margin="0,26,0,0" Height="38" VerticalAlignment="Top"/>
                </Grid>
            </Button>
            <AppBarSeparator/>
            <Button Width="150" Click="ConnectToRemoteServerButton_Click" Height="150" VerticalAlignment="Top" HorizontalAlignment="Center">
                <Grid>
                    <FontIcon Glyph="&#xE13C;" Margin="0,-28,0,28" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="Connect to a Remote Server" TextWrapping="Wrap" Margin="0,26,0,0" Height="38" VerticalAlignment="Top"/>
                </Grid>
            </Button>
        </StackPanel>
    </Grid>
</Page>
