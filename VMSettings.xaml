<ContentDialog
    x:Class="UWPStation.VMSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPStation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    mc:Ignorable="d"
    Loaded="ContentDialog_Loaded"
    CornerRadius="8">

    <ContentDialog.Resources>
        <ResourceDictionary>
            <x:Double x:Key="ContentDialogMaxWidth">1100</x:Double>
            <Thickness x:Key="ContentDialogPadding">0</Thickness>
        </ResourceDictionary>
    </ContentDialog.Resources>
    <Grid x:Name="ContainerGrid"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
		  Width="800"
		  Height="760"
		  HorizontalAlignment="Stretch"
		  VerticalAlignment="Stretch">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.ThemeDictionaries>
                    <ResourceDictionary x:Key="Dark">
                        <Color x:Key="SystemAltHighColor">#1D1D1D</Color>
                        <SolidColorBrush x:Key="ApplicationPageBackgroundThemeBrush" Color="{ThemeResource SystemAltHighColor}"/>
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="Light">
                        <Color x:Key="SystemAltHighColor">White</Color>
                        <SolidColorBrush x:Key="ApplicationPageBackgroundThemeBrush" Color="{ThemeResource SystemAltHighColor}"/>
                    </ResourceDictionary>
                </ResourceDictionary.ThemeDictionaries>
            </ResourceDictionary>
        </Grid.Resources>
        <ScrollViewer Margin="0,0,0,50" VerticalScrollBarVisibility="Hidden">
            <Frame x:Name="ContentFrame"/>
        </ScrollViewer>
        <Grid x:Name="BottomGrid" VerticalAlignment="Top" Height="50" Margin="0,710,0,0">
            <Button	x:Name="BackButton" Content="Back"
		            Height="35" IsTabStop="False"
		            AutomationProperties.Name="Back"
                    IsEnabled="{x:Bind ContentFrame.CanGoBack, Mode=OneWay}"
	             	Click="BackButton_Click" UseSystemFocusVisuals="False" VerticalContentAlignment="Stretch"
	               HorizontalContentAlignment="Stretch" Margin="10,0,0,10" VerticalAlignment="Bottom">
            </Button>
            <Button x:Name="OKButton" Content="OK" Margin="619,0,0,10" VerticalAlignment="Bottom" Height="35" UseSystemFocusVisuals="False" Click="OKButton_Click" IsTabStop="False"/>
            <Button	x:Name="CancelButton" Content="Cancel" Height="35" Click="CancelButton_Click" UseSystemFocusVisuals="False" Margin="667,0,0,10" VerticalAlignment="Bottom" IsTabStop="False"/>
            <Button x:Name="HelpButton" Content="Help" Height="35" UseSystemFocusVisuals="False" Margin="737,0,0,10" VerticalAlignment="Bottom" IsTabStop="False"/>
        </Grid>
    </Grid>
</ContentDialog>
